name: VP8L v4 clean candidate matrix
on:
  push:
    branches: [bench-vp8l-v4-clean-matrix]
    paths: [tools/bench_vp8l_v4_clean_matrix.py, .github/workflows/bench-vp8l-v4-clean-matrix.yml]
permissions: { contents: write }
jobs:
  bench:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with: { fetch-depth: 0 }
      - uses: dtolnay/rust-toolchain@stable
        with: { components: clippy,rustfmt }
      - run: rustup toolchain install 1.80.1 --profile minimal
      - run: |
          sudo apt-get update -qq
          sudo apt-get install -y webp
      - run: python3 tools/bench_vp8l_v4_clean_matrix.py
      - run: |
          git config user.name github-actions[bot]
          git config user.email 41898282+github-actions[bot]@users.noreply.github.com
          git add benchmark-vp8l-v4-clean-matrix.md
          git commit -m 'Record VP8L v4 clean candidate matrix'
          git push origin HEAD:bench-vp8l-v4-clean-matrix
