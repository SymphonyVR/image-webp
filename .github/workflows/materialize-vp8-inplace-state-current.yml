name: Materialize in-place VP8 arithmetic state on current tree
on:
  push:
    branches: [ candidate-vp8-inplace-state-current ]
    paths: [ .github/workflows/materialize-vp8-inplace-state-current.yml ]
permissions:
  contents: write
jobs:
  apply:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with: { fetch-depth: 0 }
      - uses: dtolnay/rust-toolchain@stable
        with: { components: clippy,rustfmt }
      - name: Apply exact tested arithmetic transformation
        shell: bash
        run: |
          set -euxo pipefail
          python3 .github/scripts/apply_vp8_inplace_state_current.py
          cargo fmt
          git diff --check
      - name: Verify combined tree
        shell: bash
        run: |
          set -euxo pipefail
          cargo test -v
          cargo doc -v
          cargo clippy --all-features -- -D warnings
          cargo fmt -- --check
          rustup toolchain install 1.80.1 --profile minimal
          cargo +1.80.1 build -v
      - name: Publish source-only candidate
        shell: bash
        run: |
          set -euxo pipefail
          git config user.name github-actions[bot]
          git config user.email 41898282+github-actions[bot]@users.noreply.github.com
          git add src/lossy/arithmetic_decoder.rs
          git commit -m 'Update VP8 arithmetic state in place'
          git rm .github/workflows/materialize-vp8-inplace-state-current.yml .github/scripts/apply_vp8_inplace_state_current.py
          git commit -m 'Finalize in-place VP8 arithmetic state optimization'
          git push origin HEAD:candidate-vp8-inplace-state-current
